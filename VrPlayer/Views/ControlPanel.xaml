<UserControl x:Class="VrPlayer.Views.ControlPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="boolToVis"/>
    </UserControl.Resources>
    <StatusBar Visibility="{Binding Path=HasDuration, Converter={StaticResource boolToVis}}">
        <StatusBarItem DockPanel.Dock="Left">
            <WrapPanel>
				<Button Height="25" Width="25" Command="{Binding PlayCommand}">
					<Image Source="/Medias/player_play.png"></Image>
				</Button>
				<Button Height="25" Width="25" Command="{Binding PauseCommand}">
					<Image Source="/Medias/player_pause.png"></Image>
				</Button>
				<Button Height="25" Width="25" Command="{Binding StopCommand}">
					<Image Source="/Medias/player_stop.png"></Image>
				</Button>
			</WrapPanel>
        </StatusBarItem>
        <StatusBarItem HorizontalContentAlignment="Stretch">
            <ProgressBar Height="25" HorizontalAlignment="Stretch" Value="{Binding Path=Progress}" Minimum="0" Maximum="100" MouseDown="ProgressBar_MouseDown" MouseMove="ProgressBar_MouseMove"/>
        </StatusBarItem>
    </StatusBar>
</UserControl>
