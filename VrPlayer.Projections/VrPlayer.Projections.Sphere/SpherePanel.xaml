<UserControl x:Class="VrPlayer.Projections.Sphere.SpherePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <StackPanel Orientation="Vertical" Margin="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" MinWidth="200" />
                <ColumnDefinition Width="50" />
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Grid.Row="0" Content="Slices:"></Label>
            <Slider Grid.Column="1" Grid.Row="0" Value="{Binding Path=Slices}" Minimum="8" Maximum="256"></Slider>
            <TextBox Grid.Column="2" Grid.Row="0" Text="{Binding Path=Slices}" ></TextBox>
            <Label Grid.Column="0" Grid.Row="1" Content="Stacks:"></Label>
            <Slider Grid.Column="1" Grid.Row="1" Value="{Binding Path=Stacks}" Minimum="8" Maximum="256"></Slider>
            <TextBox Grid.Column="2" Grid.Row="1" Text="{Binding Path=Stacks}"></TextBox>
            <Label Grid.Column="0" Grid.Row="2" Content="Width:"></Label>
            <Slider Grid.Column="1" Grid.Row="2" Value="{Binding Path=Width}" Minimum="0" Maximum="2"></Slider>
            <TextBox Grid.Column="2" Grid.Row="2" Text="{Binding Path=Width}"></TextBox>
            <Label Grid.Column="0" Grid.Row="3" Content="Height:"></Label>
            <Slider Grid.Column="1" Grid.Row="3" Value="{Binding Path=Height}" Minimum="0" Maximum="2"></Slider>
            <TextBox Grid.Column="2" Grid.Row="3" Text="{Binding Path=Height}"></TextBox>
            <Label Grid.Column="0" Grid.Row="4" Content="Depth:"></Label>
            <Slider Grid.Column="1" Grid.Row="4" Value="{Binding Path=Depth}" Minimum="0" Maximum="2"></Slider>
            <TextBox Grid.Column="2" Grid.Row="4" Text="{Binding Path=Depth}"></TextBox>
        </Grid>
    </StackPanel>
</UserControl>
